import 'package:flutter/material.dart';

class AppColors {
  // Primary theme color
  static const Color primaryColor = Color(0xFF6C63FF); // Purple-ish
  static const Color secondaryColor = Color(0xFF00C896); // Teal/Green
  static const Color accentColor = Color(0xFFFFB800); // Orange/Yellow accent

  // Text colors
  static const Color textColor = Color(0xFF333333); // Dark grey
  static const Color lightTextColor = Color(0xFF888888); // Light grey
  static const Color whiteText = Colors.white;

  // Background colors
  static const Color backgroundColor = Color(0xFFF6F6F6); // Light background
  static const Color cardColor = Colors.white;

  // Button colors
  static const Color buttonColor = primaryColor;
  static const Color buttonTextColor = Colors.white;

  // Border and divider
  static const Color borderColor = Color(0xFFE0E0E0);
  static const Color dividerColor = Color(0xFFE0E0E0);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);

  // Input fields
  static const Color inputFillColor = Color(0xFFF0F0F0);
  static const Color inputBorderColor = Color(0xFFCCCCCC);
}
